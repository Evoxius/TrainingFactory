security:
    encoders:
        Symfony\Component\Security\Core\User\User: plaintext
        App\Entity\Member:
            algorithm: bcrypt  
    providers:
        chain_provider:
            chain:
                providers: [in_memory, user_db] 
        in_memory:
            memory:
                users:
                    apotheker: { password: test, roles: [ 'ROLE_MEMBER' ] }  
                    verzekering: { password: test, roles: [ 'ROLE_MEMBER' ] }          
        user_db:
            entity:
                class: App\Entity\Member
                property: username 

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: true

            form_login:
                login_path: test
                check_path: test
                default_target_path: /home/test
                provider: chain_provider

            logout:
                path: /logout
                target: /home/test
            
    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
         - { path: ^/login|home|huis_regels|privacy_beleid|cookies|contact, roles: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/ , roles: ROLE_MEMBER }
         
         
