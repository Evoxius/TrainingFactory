{% extends 'base.html.twig' %}

{% block title %}Registreren{% endblock %}

{% block body %}
<div class="container">
    <div class="card text-white bg-dark" style=" margin-bottom:25px; margin-top:80px;">
        <div class="card-header">Registreren
        </div>
        <div class="card-body">
            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>
</div>
{% endblock %}